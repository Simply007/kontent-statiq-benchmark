@model IEnumerable<Kentico.Kontent.Benchmark.Statiq.Models.Article>
@{
    ViewBag.Title = "Listing articles";
}

<h2>@ViewBag.Title</h2>
<ul>
    @foreach (var item in Model)
    {
        <li>
            <a href="./@item.Slug">@item.Title</a>
        </li>
    }
</ul>